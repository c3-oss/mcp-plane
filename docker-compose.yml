# Stub Compose file — uncomment services as you add dependencies (Postgres,
# Redis, MinIO, etc). The mcp-plane service builds the local image from this
# repo's Dockerfile.
services:
  mcp-plane:
    build:
      context: .
      target: mcp-plane
    image: mcp-plane:local
    # ports:
    #   - "8080:8080"
    # env_file:
    #   - .env
    # depends_on:
    #   - postgres

  # postgres:
  #   image: postgres:16-alpine
  #   environment:
  #     POSTGRES_USER: mcp-plane
  #     POSTGRES_PASSWORD: mcp-plane
  #     POSTGRES_DB: mcp-plane
  #   ports:
  #     - "5432:5432"
  #   volumes:
  #     - postgres-data:/var/lib/postgresql/data

# volumes:
#   postgres-data:
